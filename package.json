{
  "name": "vigorous-visual",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node -e \"const fs = require('fs'); const routes = { version: 1, include: ['/*'], exclude: ['/_astro/*', '/favicon.ico', '/favicon.svg', '/robots.txt', '/sitemap*.xml', '/*.css', '/*.js'] }; fs.writeFileSync('dist/client/_routes.json', JSON.stringify(routes, null, 2));\"",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.2",
    "@astrojs/sitemap": "^3.7.4",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.3",
    "tailwindcss": "^4.3.3",
    "tldts": "^7.4.11"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  },
  "allowScripts": {
    "esbuild": true
  }
}